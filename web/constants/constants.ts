export const LocalStorageKeys = {
  user: 'appIdHere.user',
  token: 'appIdHere.token',
};

export const DEFAULT_AUTH_PROVIDER = 'local';

/**
 * The delay in milliseconds before the side list panel is closed.
 *
 * Default: 250
 */
export const SIDE_LIST_PANEL_CLOSE_DELAY = 250;

/**
 * The delay in milliseconds for warning toast messages.
 *
 * Default: 6000
 */
export const TOAST_DELAY = 6000;

/**
 * The delay in milliseconds before automatically closing a warning toast.
 *
 * Default: 3000
 */
export const TOAST_CLOSE_DELAY = 3000;

/**
 * The maximum size of file that can be uploaded in bytes.
 *
 * Default: 20MB
 */
export const MAX_FILE_SIZE = 20 * 1024 * 1024;

/**
 * The maximum number of files that can be uploaded at a time.
 *
 * Default: 10
 */
export const FILE_LIMIT = 10;
